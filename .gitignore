# Lixo do macOS
.DS_Store

# Projeto Xcode: gerado pelo xcodegen a partir de ios/project.yml.
# A fonte da verdade é o project.yml — o .xcodeproj é derivado.
ios/*.xcodeproj/

# Datasets: a regra de verdade está em research/datasets/.gitignore

# Log do backup automatico (launchd)
research/scripts/backup.log

# Fotos pessoais do HeRo. Este repositorio e PUBLICO; a foto de capa e os
# icones recortados dela sao do Helio e da Roberta e vivem so no Artifact
# privado. Se um dia forem publicados, e por decisao explicita dos dois.
hero/img/
